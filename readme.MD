freemodbus example code ported to Qt5

This package supports CMAKE so running cmake will create the Visual studio c++ project file.

There is no GUI part in this package but will be some simple GUI to support user interaction with devices.

